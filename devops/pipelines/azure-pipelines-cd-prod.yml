trigger: none

stages:
  - stage: Deploy_Prod
    displayName: Deploy to Fabric Prod
    jobs:
      - deployment: deploy_prod
        displayName: Deploy Prod Workspace
        environment: prod
        strategy:
          runOnce:
            deploy:
              steps:
                - script: echo "Authenticate to Fabric and deploy Prod artifacts"
                  displayName: Fabric Prod deployment placeholder
