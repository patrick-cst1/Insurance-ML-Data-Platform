# Simplified dependencies for Microsoft Fabric
# Note: These are pre-installed in Fabric runtime - listed for reference only

pyspark==3.5.0
delta-spark==3.0.0

# All development and testing are performed directly in Microsoft Fabric workspace
# No framework libraries or external dependencies required
# Deployment via Fabric Git Integration (recommended)
